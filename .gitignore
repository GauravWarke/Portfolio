# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp

# Node (only if you use the React/Vite version in src/)
node_modules/
dist/
.vercel/
*.log

# Local launch + staging (not part of the portfolio repo)
.claude/
github-launch/

# Data pipeline: runtime cache + generated DuckDB file (regenerated by scripts/)
data/.cache/
*.duckdb
*.duckdb.wal

# Python
__pycache__/
*.py[cod]
.ruff_cache/

# R analysis: rendered report is committed by CI (see render-r-report.yml).
# assets/charts/ is generated by R + Python but IS tracked — the site serves it.
analysis/report_files/
.Rproj.user/
.Rhistory

# Superseded by assets/charts/ (old R output path)
analysis/figures/
